# LiveReload Protocol Fixtures

Fixtures are sanitized, small, and versioned. Each file name begins with `FXT-NNN`, identifies its direction and scenario, and includes an expected parser/encoder result in its JSON payload or adjacent Markdown note.

Do not add browser bundles, credentials, private paths, or unrelated source files.
