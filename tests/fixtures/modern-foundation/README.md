# Modern Foundation Fixtures

Fixtures are synthetic, repository-safe inputs for configuration, project identity, activity redaction, persistence recovery, and UI tests. They must contain no credentials, personal absolute paths, bookmark bytes from private folders, or source contents. Each fixture documents its expected result beside the data or in its test.
