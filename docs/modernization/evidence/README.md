# Phase 0 Evidence

Store reproducible, sanitized outputs under a topic directory such as `browser/`, `websocket/`, `monitoring/`, `signing/`, or `legacy-app/`. Every artifact must identify the related task ID, command/procedure, tool and OS version where relevant, date, and limitations.

Raw credentials, private source contents, and unnecessary absolute paths are prohibited.
